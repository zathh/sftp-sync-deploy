{
  "name": "sftp-sync-deploy",
  "version": "0.9.0",
  "description": "Sync local files to remote using SFTP.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && tsc -d",
    "prepare": "npm run build"
  },
  "author": "wangyucode",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wangyucode/sftp-sync-deploy.git"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "lib/*.js",
    "lib/*.d.ts",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@types/minimatch": "^3.0.5",
    "@types/node": "^16.11.7",
    "@types/ssh2": "^0.5.52",
    "chalk": "^5.0.1",
    "minimatch": "^5.0.1",
    "queue": "^6.0.2",
    "ssh2": "^1.10.0"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
